class Config {
  static String url =
      'https://authflutter.onrender.com/'; //recommended for running in emulator ipconfig insted localhost
  static String loginUrl = url + 'api/v1/login/';
  static String registerUrl = url + 'api/v1/register/';
}
